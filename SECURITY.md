# Security Policy

## 适用范围

ESP32 FSR MCP Bridge 面向可信家庭局域网使用，不是公网服务。

## 已知安全边界

- Android App 本地 MCP 服务默认监听 `9333` 端口，没有身份验证。
- MCP 服务当前使用 HTTP 明文传输，没有 HTTPS 加密。
- ESP32 HTTP API 没有访问鉴权，同一局域网内设备理论上可以读取传感器数据。
- BLE 配网阶段会近距离传输 WiFi 名称和密码，请避免在公共场合配网。

## 使用建议

- 不要把手机 MCP 端口映射到公网。
- 不要把 ESP32 HTTP 端口映射到公网。
- 使用可信 WiFi，避免连接开放公共网络。
- 如果发现手机或路由器提示未知设备访问，请先关闭 MCP 服务并检查局域网环境。

## 报告安全问题

请通过邮箱联系作者：

```text
xianeshijie@outlook.com
```

报告时请尽量包含复现步骤、影响范围和相关日志。
